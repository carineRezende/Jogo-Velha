# Jogo da Velha (Tic-Tac-Toe) 
## Descrição do projeto

Este projeto consiste na criação do Jogo da Velha (Tic-Tac-Toe) utilizando React.
A proposta faz parte da prática “Desenvolvendo sua primeira aplicação React”, com o objetivo de aplicar os conceitos fundamentais da biblioteca, como componentização, estado (useState), props e manipulação de eventos.

O jogo permite que dois jogadores se alternem entre “X” e “O”, exibindo o vencedor, mostrando o histórico de jogadas e possibilitando reiniciar a partida.
Além disso, o layout foi personalizado com cores, centralização e estilo uniforme para tornar a interface mais agradável.

## Tecnologias utilizadas
- React.js
- Vite
- JavaScript (ES6+)
- HTML5
- CSS3

## Instruções de instalação e execução
### 1️⃣ Pré-requisitos

Antes de começar, é necessário ter instalado em sua máquina:
- Node.js (versão 18 ou superior)
- npm (gerenciador de pacotes que vem junto com o Node)

Para verificar se estão instalados corretamente, execute no terminal:

```
node -v
npm -v
```
### 2️⃣ Instalar o Vite (caso ainda não tenha um projeto criado)

O Vite é a ferramenta usada para criar e rodar o projeto React de forma rápida e simples.

Para criar um novo projeto React com o Vite, use o comando:

```
npm create vite@latest
```

➡️ Depois disso, o terminal vai te perguntar:
- Nome do projeto
- Framework (escolha React)
- Variante (escolha JavaScript)
- Essa é a forma ideal pra aprender — você vê todas as opções.

Depois, entre na pasta criada:

```
cd nome-do-projeto
```

E instale as dependências:

```
npm install
```

### 3️⃣ Executar o projeto

Para rodar o projeto localmente:

```
npm run dev
```

O terminal exibirá um link semelhante a:

http://localhost:5173

Abra esse link no navegador para visualizar o jogo funcionando 🎮

## Principais aprendizados
- Durante o desenvolvimento do projeto, foram praticados os seguintes conceitos:
- Criação e uso de componentes React.
- Passagem de informações por meio de props.
- Controle de estado com o hook useState.
- Manipulação de eventos e atualização dinâmica da interface.
- Estruturação e estilização do layout com CSS.
- Entendimento do ciclo de renderização e da reatividade do React.

## Créditos e autoria

Autora: Carine Rezende
Disciplina: Programação Web
Instituição/Professor: Leonardo
Ferramenta utilizada: Vite
